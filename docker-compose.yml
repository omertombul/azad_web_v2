services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: azad
      POSTGRES_PASSWORD: azad
      POSTGRES_DB: azad
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
